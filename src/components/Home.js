import React from 'react';
import './Home.css';

const Home = () => (
  <>
    <div className="home">
      <h1> Welcome To Our Page! </h1>
      <p>
        Math Magicians is a website that built using React and Redux.
        It is a simple calculator that can perform basic math operations, try it out now!
        the whole website was built using SPA (Single Page Application) technique.
        routes are handled by react-router-dom.
        all the components are functional components.
        the state is managed by redux.
      </p>
    </div>
  </>
);

export default Home;
